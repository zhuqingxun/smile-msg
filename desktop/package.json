{
  "name": "desktop",
  "version": "1.0.0",
  "private": true,
  "main": "./out/main/index.js",
  "scripts": {
    "dev": "electron-vite dev",
    "build": "electron-vite build",
    "postinstall": "electron-builder install-app-deps",
    "prebuild:win": "taskkill /F /IM SmileMsg.exe 2>nul || echo SmileMsg not running",
    "build:win": "electron-vite build && electron-builder --win --config electron-builder.yml",
    "postbuild:win": "powershell -ExecutionPolicy Bypass -File scripts/postbuild-win.ps1"
  },
  "dependencies": {
    "@electron-toolkit/preload": "^3.0.1",
    "@electron-toolkit/utils": "^4.0.0",
    "socket.io-client": "^4.8.0",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "@vitejs/plugin-vue": "^5.2.0",
    "electron": "^33.0.0",
    "electron-builder": "^25.1.0",
    "electron-vite": "^3.0.0",
    "tailwindcss": "^4.0.0",
    "vite": "^6.1.0"
  }
}
